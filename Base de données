import java.sql.*;

public class BaseDonnees {
    private static final String URL = "jdbc:sqlite:dbecoles.db";
    public static Connection getConnection() {
        try{
            Class.forName("org.sqlite.JDBC");
            return DriverManager.getConnection(URL);
        }catch (ClassNotFoundException e){
            System.out.println("le pilote SQLite introuvable" + e.getMessage());
            return null;
        }catch (SQLException e){
            System.out.println("Erreur de Connexion a la BD: " + e.getMessage());
            return null;
        }
    }

    public static void activeWAL(){
        try(Connection conn = getConnection();
            Statement stmt = conn.createStatement()){
                stmt.execute("PRAGMA journal_mode=WAL;");
                System.out.println("Mode WAL active");
            }catch(Exception e){
                System.out.println("Impossible d'active WAL");
            }
    }
    

    // public static void initialiserBD(){
    //     String creatables = """
    //             CREATE TABLE IF NOT EXISTS utilisateurs (
    //             id INTEGER PRIMARY KEY AUTOINCREMENT,
    //             nom VARCHAR(50) NOT NULL,
    //             prenom VARCHAR(50) NOT NULL,
    //             email VARCHAR(100)  UNIQUE NOT NULL,
    //             mot_passe VARCHAR(10) NOT NULL,
    //             role VARCHAR(20) NOT NULL
    //             );
    //             CREATE TABLE IF NOT EXISTS eleves (
    //             matricule  VARCHAR(20) PRIMARY KEY,
    //             nom VARCHAR(50) NOT NULL,
    //             prenom VARCHAR(50) NOT NULL,
    //             datenaissance VARCHAR(20) NOT NULL,
    //             classe VARCHAR(20) NOT NULL,
    //             statut VARCHAR(20) NOT NULL
    //             );
    //              CREATE TABLE IF NOT EXISTS notes (
    //             id INTEGER PRIMARY KEY AUTOINCREMENT, 
    //             eleve_id VARCHAR(20) NOT NULL,
    //             matiere VARCHAR(50) NOT NULL,
    //             note FLOAT NOT NULL,
    //             coefficient INT NOT NULL,
    //             apreciation VARCHAR(20) NOT NULL,
    //             FOREIGN KEY (eleve_id) REFERENCES eleves(matricule)
    //             );
                 
    //             """;
    //     try{
    //         Connection conn = getConnexion();
    //         Statement stmt = conn.createStatement();
    //             stmt.execute(creatables);
    //             System.out.println("Creation tables reussie !");

    //         }catch (SQLException e){
    //             System.out.println("Erreur de connexion a la BD");
    //         }   
    // }

    // public static void main(String[] args){
    //     initialiserBD();
    // }
}
